Edit the attached PDblend figure. Keep its successful exact layout, English text, numbers, white background, serif typography, four-panel structure and green/blue/yellow/gray palette. Make a targeted technical correction to CONNECTORS AND TIME ANNOTATIONS only, with a little extra spacing where needed. Produce a fully finished corrected research-paper figure.

1. Fix routing arrows: The right-pointing arrow from the box "Least-loaded M: I4" currently incorrectly aligns with the long P runtime row. Route this connector upward through an empty gutter and end it next to the SHORT-request subtitle/upper example. The arrow from "P: I0 → compatible D: I2" should end next to the LONG-request subtitle/lower example, not at the Client row. Move the gutter or panel boundary slightly if necessary; no arrow through any text. The Joint Plan down-arrow should lead to the ready-plan grid in panel (b), not point into blank space above panel (c). Use an outer whitespace elbow connector if necessary.

2. In the upper short-request example, the red dashed TTFT interval must end at the actual horizontal position of the yellow y₁ client token, not at the end of the green runtime bar. Keep the thin arrow from end of green prefill to y₁. Replace the current blue dashed "Inter-token gap" annotation (incorrectly spans from y₃ to an ellipsis) with a double-headed dashed blue bracket exactly BETWEEN y₂ AND y₃. Put its label on its own clear line. No timing bracket can terminate at an ellipsis.

3. In the lower long-request example, a D decode step produces a token at the END of the blue rectangle. Move the fine arrow to client y₂ to start at the RIGHT edge of the FIRST blue decode cell after the gray handoff block, rather than its current start at the gray/blue boundary. Likewise point from the end of the second blue cell to client y₃. Show y₂ and y₃ sufficiently spaced to place an "Inter-token gap" blue double-headed dashed bracket exactly between them; remove the current erroneous bracket from y₃ toward an ellipsis. Keep the client y₁ under end of green P, before y₂. Gray/black dashed "Continuation gap" must span y₁ to y₂. Red TTFT ends at y₁. Keep the correct TPOT formula and all exact prompt/KV/budget numbers. Keep protocol annotation clear of token arrows.

4. Fix feedback geometry: One dashed observation-feedback connector should originate from the "Observe TTFT / TPOT / inflight" box in (d) and return to "Feasible-plan search" in (a), labeled "Slower replanning". It should use an outer margin. Remove the extraneous dangling dashed arrow above the top boxes and the current feedback line connecting the risk box to Joint Plan. Keep all solid control-sequence arrows.

Strictly preserve content and style otherwise. Keep the caption stating that the plan and timelines are illustrative. Make all connectors unambiguous, uncluttered and technically faithful. Prefer a large high-resolution output, 3072 x 2048 if available.
